spring:
  datasource:
    url: jdbc:postgresql://aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres # 데이터베이스 URL
    username: postgres.dfbmjhsgjeikaamldzha # 데이터베이스 사용자 이름
    password: MmhzOUyRR89CgsgR # 데이터베이스 비밀번호
    driver-class-name: org.postgresql.Driver # PostgreSQL 드라이버 클래스 이름

  # 시큐리티
  security:
    user:
      name: tester
      password: Password1234!

# 로깅 설정
logging:
  level:
    #    org.example.bootsecurity.model.mapper: DEBUG # mapper 패키지의 로깅 레벨을 DEBUG로 설정 (DEV)
    org.example.bootsecurity.model.mapper: ERROR # mapper 패키지의 로깅 레벨을 ERROR로 설정
    # 너무 쓸데없는 많은 로그 방지